# 부자타임 애드센스 승인 점검 보고서 + 작업 체크리스트

- 점검일: 2026-02-22
- 목적: 애드센스 심사 승인 가능성 개선 및 재신청 전 필수 작업 정리
- 현재 판단: **현 상태로는 승인 가능성 낮음(치명 이슈 존재)**

---

## 1) 이번 점검 핵심 결론

### 치명 이슈 (반드시 선조치)
- [x] 소스/메타 문자열 깨짐 및 문법 손상 복구 (UTF-8, 따옴표/태그/템플릿 문자열 정상화)
- [x] `posts.json` vs `categories.json` 불일치 해소 (`digital-skill` 카테고리 매핑 문제)
- [x] `draft` 카테고리의 색인/사이트맵 노출 제거
- [x] 중복 slug 제거 (`kakao-taxi-guide` 중복)

### 중요 이슈 (승인률 상승에 직접 영향)
- [x] 푸터 내부 링크와 실제 카테고리 정합성 맞추기
- [x] RSS/프리렌더 대상에서 비공개성/운영성 콘텐츠 정책 정리
- [ ] 작성자/운영자 신뢰 정보 보강(placeholder 제거, 이미지/프로필 실체화)

---

## 2) 점검 근거(파일 기준)

### A. 소스 손상/문법 리스크
- `index.html`
  - 메타 값 일부 따옴표 깨짐/문자열 손상 정황
- `src/components/common/SEOHead.tsx`
  - 사이트명 상수 문자열 손상 정황
- `src/pages/CategoryPage.tsx`
  - `title` 템플릿 문자열 손상 정황
- `scripts/generate-rss.ts`
  - 마지막 `console.log` 문자열 구문 손상 정황

### B. 데이터 정합성 리스크
- `src/hooks/usePosts.ts`
  - `categories.find(...)!` 강제 단정으로 미존재 카테고리 시 런타임 실패 가능
- `src/data/categories.json`
  - `digital-skill` 카테고리 부재
- `src/data/posts.json`
  - `categoryId: "digital-skill"` 포스트 4건 존재
  - `slug: "kakao-taxi-guide"` 중복 2건 존재

### C. 색인/품질 리스크
- `scripts/generate-sitemap.ts`
  - `briefing`만 제외, `draft` 제외 로직 없음
- `public/sitemap.xml`
  - `/blog/category/draft` 노출 확인
- `src/components/layout/Footer.tsx`
  - 실제 데이터에 없는 카테고리 링크 다수 포함 가능성

---

## 3) 승인 목표 작업 체크리스트 (실행 순서)

## Phase 1. 치명 이슈 제거 (D-3 ~ D-2)
- [x] 전체 소스 인코딩 통일: UTF-8(BOM 없음)
- [x] `index.html` 메타 태그/따옴표/문자열 정상화
- [x] `src/components/common/SEOHead.tsx` 문자열/메타 생성 정상화
- [x] `src/pages/CategoryPage.tsx` 템플릿 문자열 구문 정상화
- [x] `scripts/generate-rss.ts` 구문 오류 수정
- [x] `src/data/categories.json`에 실제 사용 카테고리 구조 확정
- [x] `src/data/posts.json`의 `categoryId` 전수 검증 및 수정
- [x] 중복 slug 제거 (`kakao-taxi-guide`)
- [x] `usePosts`에서 카테고리 미일치 시 안전 처리(에러 격리 + 로그)

완료 기준
- [ ] 로컬 빌드/배포 빌드에서 구문 에러 0
- [ ] 포스트 목록/상세 정상 노출, 빈 화면/크래시 0

## Phase 2. 색인/정책 정리 (D-2 ~ D-1)
- [x] `draft`, `briefing` 공개/비공개 정책 확정
- [x] 사이트맵 생성 시 `draft`, `briefing` 제외 반영
- [x] RSS 생성 시 제외 대상 반영
- [x] 카테고리 페이지 `noindex` 정책 점검
- [x] 푸터/내부 링크를 실제 존재 카테고리만 노출하도록 수정
- [ ] `robots.txt` 재검토 (`/admin` 차단 유지)

완료 기준
- [ ] `public/sitemap.xml`에서 `draft`/비공개성 경로 미노출
- [ ] 깨진 내부링크 0

## Phase 3. 신뢰성 보강 (D-1)
- [x] 작성자 정보 placeholder(`example`) 제거
- [x] 작성자 이미지 실제 파일 배치 및 깨진 이미지 0
- [x] About/Contact/Privacy/Terms 텍스트 가독성 최종 점검
- [ ] 운영 주체(상호/연락처/사업자 정보) 일관성 검증

완료 기준
- [ ] 사이트 전 페이지에서 정책/문의/운영자 정보 접근 가능
- [ ] 신뢰 신호(운영자/작성자) 불명확 요소 0

## Phase 4. 재신청 직전 점검 (당일)
- [ ] `ads.txt` 퍼블리셔 ID 재확인
- [ ] Search Console 색인 요청(홈/블로그/대표 포스트/도구)
- [ ] 주요 URL 모바일 렌더 확인(홈, 블로그, 포스트 3개, 도구 3개)
- [ ] 페이지 속도/오류/콘솔 에러 점검
- [ ] 최종적으로 광고 코드 중복 삽입 여부 확인

완료 기준
- [ ] 크롤링 가능 + 콘텐츠 가독성 + 정책 페이지 완비
- [ ] 신청 전 체크리스트 100% 완료

---

## 4) 기술 검증 체크리스트(개발용)

- [x] `posts.json` 고유성 검사: slug 중복 0
- [x] `posts.json` 참조 무결성: 모든 `categoryId`, `authorId` 유효
- [ ] `sitemap.xml` URL 유효성: 200 또는 의도된 301만 허용
- [ ] 404/리다이렉트 체인 과다 URL 제거
- [ ] SSR/프리렌더 결과물의 title/description/canonical 검증

권장 자동화
- [x] CI에 데이터 무결성 스크립트 추가(slug/category/author 검사)
- [ ] CI에 sitemap URL 검증 스크립트 추가

---

## 5) 재신청 준비 상태 트래커

- 오늘(2026-02-22) 기준 상태: **Blocked**
- Blocker
  - [x] 데이터 정합성 오류
  - [x] 색인 정책 미정리(`draft` 노출)
  - [ ] 문자열/구문 손상 의심 파일 다수

재신청 조건(모두 충족 시 신청)
- [ ] Blocker 0
- [ ] 핵심 페이지 품질 점검 완료
- [ ] Search Console 인덱싱 재요청 완료

---

## 6) 작업 로그

- 2026-02-22: 1차 전체 점검 문서화 완료
- 2026-02-22: Phase 1 핵심 수정 반영
  - `digital-skill` 카테고리 추가 및 `categoryId` 참조 무결성 정상화
  - 중복 slug(`kakao-taxi-guide`) 제거
  - 사이트맵/RSS/프리렌더에서 `draft`, `briefing` 제외 + slug 중복 방지
  - 푸터 카테고리 링크를 실제 존재 카테고리로 정렬
  - 포스트 로딩 시 잘못된 메타데이터를 건너뛰는 안전 처리 추가
- 2026-02-22: Phase 3 신뢰 신호 보강 반영
  - 작성자 `example` 소셜 링크 제거 및 실 연락 이메일 통일
  - 작성자 이미지 자산(`public/images/authors/*.svg`) 추가 및 폴백 적용
  - 콘텐츠 무결성 점검 스크립트 `scripts/validate-content.mjs` 추가
  - `npm run check:content` 통과 확인
- 다음 업데이트: 실제 수정 커밋 반영 후 본 문서 체크박스 갱신
